from pocha import describe, it, before, after
from expects import expect, equal, be_none, be, be_above, be_true, have_keys


@describe('framework recommendation model', skip=True)
def _():

    @it('does stuff', skip=True)
    def _():
        pass
